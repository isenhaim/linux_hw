# 1. Просмотреть содержимое директорий /etc, /proc, /home. Посмотреть пару произвольных файлов в /etc.

ls -la /etc
ls -la /proc
ls -la /home

cat /etc/ucf.conf
cat /etc/logrotate.conf

# 2. Выяснить, для чего предназначена команда cat. Используя данную команду, создайте два файла с данными, а затем объедините их в один. 
# Просмотрите содержимое созданного файла. Переименуйте файл, дав ему новое имя.

man cat

cat > /home/isenhaim/example1.txt
1
2
3
4
^C 

cat > /home/isenhaim/example2.txt
a
b
c
d
^C 

cd /home/isenhaim
# для объединения двух файлов в третий 
cat example1.txt example2.txt > example3.txt

# для объединения в одном из уже созданных
cat example1.txt >> example2.txt

mv example3.txt rename_file.txt

# 3. Создать несколько файлов. Создайте директорию, переместите файл туда. 
# Удалите все созданные в этом и предыдущем задании директории и файлы.

touch ex1.txt & touch ex2.txt 
mkdir home_work 
mv *.txt home_work

rm -r home_work

# 4. В ОС Linux скрытыми файлами считаются те, имена которых начинаются с символа “.”. 
# Сколько скрытых файлов в вашем домашнем каталоге? (Использовать конвейер. 
# Подсказка: для подсчета количества строк можно использовать wc).

find . -maxdepth 1 -type f -name '.*' | wc -l

# ./.profile
# ./.vboxclient-display.pid
# ./.vboxclient-clipboard.pid
# ./.vboxclient-draganddrop.pid
# ./.sudo_as_admin_successful
# ./.bash_history
# ./.bash_logout
# ./.vboxclient-seamless.pid
# ./.ICEauthority
# ./.bashrc

# 10


# 5. Попробовать вывести с помощью cat все файлы в директории /etc. 
# Направить ошибки в отдельный файл в вашей домашней директории. 
# Сколько файлов, которые не удалось посмотреть, оказалось в списке?

cat /etc/* 1> ex.txt 2> err.txt
cat /etc/* 2> error.txt

cat err.txt | uniq | wc -l
127


# 6. Запустить в одном терминале программу, в другом терминале посмотреть PID процесса и остановить с помощью kill, посылая разные типы сигналов. 
#  Что происходит?

which python3

vim loop.py 

# #!/usr/bin/python3
# while True:
#    pass

chmod +x loop.py
./loop.py

top

kill -9 7117
Killed

top

kill -15 7448
Terminated

# 7. *Отобразить в /dev список устройств, которые в настоящее время используются вашим UID (для этого используется команда lsof). 
# Организовать конвейер через less, чтобы посмотреть их должным образом.

lsof /dev  | less

# 8. *Cоздайте директорию для хранения фотографий, в ней должны быть директории по годам, (например, за последние 5 лет), и в каждой директории года по директории для месяца.


isenhaim@isenhaim:~$ mkdir Photo
isenhaim@isenhaim:~$ cd Photo
isenhaim@isenhaim:~/Photo$ mkdir -p {2015..2020}/{01..12}

# 9. *Заполните директории файлами вида ГГГГММДДНН.txt. 
# Внутри файла должно быть имя файла. Например 2018011301.txt, 2018011302.txt.

touch $(pwd){01..31} | echo $(pwd) > $(pwd).txt